**Figure 1: Essential matrix recoverPose repair validation.** (a) Under a small baseline of 1.5 cm, OpenCV default `distanceThresh=50.0` fails completely (0/500 inliers), whereas repairing `distanceThresh=1000.0` achieves 100% pose recovery (500/500 inliers). Under a 20.0 cm baseline, both configurations recover all 500/500 points. (b) Empirical active-window inlier count profile on F9 R1 (mean: 564.8 inliers, valid pose fraction: 93.54%). old-threshold real-flight value not reproducible from committed data.
